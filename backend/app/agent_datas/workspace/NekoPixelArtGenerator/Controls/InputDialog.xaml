<Window x:Class="PixelArtGenerator.Controls.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="150" Width="400"
        WindowStartupLocation="CenterOwner"
        Background="#2D2D30"
        Foreground="#F1F1F1">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="{Binding Message}" 
                   Foreground="#F1F1F1" Margin="0,0,0,10"/>
        
        <TextBox Grid.Row="1" x:Name="InputTextBox" 
                 Background="#3C3C3C" Foreground="#F1F1F1" 
                 BorderBrush="#464647" Padding="5"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"/>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="确定" Width="75" Margin="0,0,5,0" 
                    Click="OKButton_Click" IsDefault="True"/>
            <Button Content="取消" Width="75" 
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>